diff --git a/edict_functions.py b/edict_functions.py
index 7278c4a..2749714 100644
--- a/edict_functions.py
+++ b/edict_functions.py
@@ -1,4 +1,5 @@
 from transformers import CLIPModel, CLIPTokenizer
+import os
 import numpy as np
 from PIL import Image
 import torch
@@ -12,15 +13,23 @@ from torch.cuda.amp import autocast
 from difflib import SequenceMatcher
 
 # Build our CLIP model
-model_path_clip = "/home/liyan/.cache/huggingface/clip-vit-large-patch14"
+model_path_clip = os.environ.get(
+    "RGS_CLIP_MODEL",
+    "/data2/liyanlei/stego_attack_models/mas_grdh/clip/clip-vit-large-patch14-local",
+)
 clip_tokenizer = CLIPTokenizer.from_pretrained(model_path_clip)
 clip_model = CLIPModel.from_pretrained(model_path_clip, torch_dtype=torch.float16)
 clip = clip_model.text_model
 
 # Getting our HF Auth token
-with open('hf_auth', 'r') as f:
-    auth_token = f.readlines()[0].strip()
-model_path_diffusion = "/home/liyan/.cache/huggingface/stable-diffusion-v1-5"
+auth_token = os.environ.get("HF_TOKEN", "")
+if not auth_token and os.path.exists("hf_auth"):
+    with open("hf_auth", "r") as f:
+        auth_token = f.readlines()[0].strip()
+model_path_diffusion = os.environ.get(
+    "RGS_SD_MODEL",
+    "/home/liyanlei/work/NS-DSer-master/sd_ckpt/stable-diffusion-v1-5",
+)
 
 # Build our SD model
 unet = UNet2DConditionModel.from_pretrained(model_path_diffusion, subfolder="unet", use_auth_token=auth_token,
@@ -32,10 +41,14 @@ vae = AutoencoderKL.from_pretrained(model_path_diffusion, subfolder="vae", use_a
 device = 'cuda'
 unet.double().to(device)
 vae.double().to(device)
-clip.double().to(device)
+clip.float().to(device)
 print("Loaded all models")
 
 
+def encode_text(input_ids):
+    return clip(input_ids.to(device)).last_hidden_state.to(unet.dtype)
+
+
 def EDICT_editing(im_path,
                   base_prompt,
                   edit_prompt,
@@ -445,11 +458,11 @@ def baseline_stablediffusion(prompt="",
         tokens_unconditional = clip_tokenizer(null_prompt, padding="max_length",
                                               max_length=clip_tokenizer.model_max_length, truncation=True,
                                               return_tensors="pt", return_overflowing_tokens=True)
-        embedding_unconditional = clip(tokens_unconditional.input_ids.to(device)).last_hidden_state
+        embedding_unconditional = encode_text(tokens_unconditional.input_ids)
 
         tokens_conditional = clip_tokenizer(prompt, padding="max_length", max_length=clip_tokenizer.model_max_length,
                                             truncation=True, return_tensors="pt", return_overflowing_tokens=True)
-        embedding_conditional = clip(tokens_conditional.input_ids.to(device)).last_hidden_state
+        embedding_conditional = encode_text(tokens_conditional.input_ids)
 
         # Process prompt editing
         assert not ((prompt_edit is not None) and (prev_image is not None))
@@ -457,7 +470,7 @@ def baseline_stablediffusion(prompt="",
             tokens_conditional_edit = clip_tokenizer(prompt_edit, padding="max_length",
                                                      max_length=clip_tokenizer.model_max_length, truncation=True,
                                                      return_tensors="pt", return_overflowing_tokens=True)
-            embedding_conditional_edit = clip(tokens_conditional_edit.input_ids.to(device)).last_hidden_state
+            embedding_conditional_edit = encode_text(tokens_conditional_edit.input_ids)
             init_attention_edit(tokens_conditional, tokens_conditional_edit)
         elif prev_image is not None:
             init_attention_edit(tokens_conditional, tokens_conditional)
@@ -821,13 +834,13 @@ def coupled_stablediffusion(prompt="",
                                           max_length=clip_tokenizer.model_max_length,
                                           truncation=True, return_tensors="pt",
                                           return_overflowing_tokens=True)
-    embedding_unconditional = clip(tokens_unconditional.input_ids.to(device)).last_hidden_state
+    embedding_unconditional = encode_text(tokens_unconditional.input_ids)
 
     tokens_conditional = clip_tokenizer(prompt, padding="max_length",
                                         max_length=clip_tokenizer.model_max_length,
                                         truncation=True, return_tensors="pt",
                                         return_overflowing_tokens=True)
-    embedding_conditional = clip(tokens_conditional.input_ids.to(device)).last_hidden_state
+    embedding_conditional = encode_text(tokens_conditional.input_ids)
 
     # Process prompt editing (if running Prompt-to-Prompt)
     if prompt_edit is not None:
@@ -835,7 +848,7 @@ def coupled_stablediffusion(prompt="",
                                                  max_length=clip_tokenizer.model_max_length,
                                                  truncation=True, return_tensors="pt",
                                                  return_overflowing_tokens=True)
-        embedding_conditional_edit = clip(tokens_conditional_edit.input_ids.to(device)).last_hidden_state
+        embedding_conditional_edit = encode_text(tokens_conditional_edit.input_ids)
 
         init_attention_edit(tokens_conditional, tokens_conditional_edit)
 
diff --git a/hide_and_reveal.py b/hide_and_reveal.py
index 6d38e1e..a192d92 100644
--- a/hide_and_reveal.py
+++ b/hide_and_reveal.py
@@ -25,6 +25,18 @@ if __name__ == '__main__':
                         help='Input image, video or folder.')
     parser.add_argument('-w', '--fidelity_weight', type=float, default=0.5,
                         help='Balance the quality and fidelity.')
+    parser.add_argument('-o', '--output_root', type=str, default=None,
+                        help='Output directory. Defaults to results_<jpeg_qf>_<gaussian_var>.')
+    parser.add_argument('--jpeg_qf', type=int, default=90,
+                        help='JPEG quality factor used for the built-in robustness check.')
+    parser.add_argument('--gaussian_var', type=float, default=0.005,
+                        help='Gaussian noise std used for the built-in robustness check.')
+    parser.add_argument('--steps', type=int, default=50,
+                        help='Diffusion steps for the official hide/reveal process.')
+    parser.add_argument('--hide_only', action='store_true',
+                        help='Only generate and save stego images; skip reveal and built-in attacks.')
+    parser.add_argument('--identity_only', action='store_true',
+                        help='Reveal only the unmodified stego image and skip built-in JPEG/Gaussian attacks.')
 
     args = parser.parse_args()
 
@@ -118,10 +130,10 @@ if __name__ == '__main__':
 
     # ------------------------ input & output ------------------------
     # 攻击强度
-    gaussian_var = 0.005
-    jpeg_qf = 90
-    results_root = f'results_{jpeg_qf}_{gaussian_var}/'
-    os.mkdir(results_root)
+    gaussian_var = args.gaussian_var
+    jpeg_qf = args.jpeg_qf
+    results_root = args.output_root or f'results_{jpeg_qf}_{gaussian_var}/'
+    os.makedirs(results_root, exist_ok=True)
 
     w = args.fidelity_weight
     if args.input_path.endswith(('jpg', 'jpeg', 'png', 'JPG', 'JPEG', 'PNG')):  # input single img path
@@ -147,7 +159,7 @@ if __name__ == '__main__':
         # 读取secret
         img_name = os.path.basename(img_path)
         basename, ext = os.path.splitext(img_name)
-        os.mkdir(os.path.join(results_root, basename))
+        os.makedirs(os.path.join(results_root, basename), exist_ok=True)
 
         print(f'[{i + 1}/{len(input_img_list)}]Secret Processing: {img_name}')
         secret_img = cv2.imread(img_path)
@@ -199,6 +211,7 @@ if __name__ == '__main__':
                                                                                  run_baseline=True,
                                                                                  guidance_scale=7.5,
                                                                                  init_noise=noise_modify,
+                                                                                 steps=args.steps,
                                                                                  )
             stego_img = generate_image[0]
 
@@ -215,6 +228,11 @@ if __name__ == '__main__':
 
             torch.cuda.empty_cache()
 
+            if args.hide_only:
+                print('hide_only: skipped reveal and built-in attacks', file=txt_file)
+                txt_file.close()
+                continue
+
         # -------------------- revealing secret ---------------------
         # 直接读取刚刚生成的stego
         stego_name = os.path.basename(save_stego_path)
@@ -240,9 +258,14 @@ if __name__ == '__main__':
         stego_jpeg_img = Image.open(save_jpeg_path)
 
         # stego进行逆向过程，恢复得到image_latent，对符号位进行提取秘密比特——→秘密比特恢复得到indice——→VQDecoder得到secret'
-        stego_with_attack = [stego_jpeg_img, stego_g_noisy_img, stego_img]  # stego_jpeg_img,
-        stego_with_attack_path = [save_jpeg_path, save_g_noisy_path, save_stego_path]  # save_jpeg_path,
-        attack = [f'jpeg_{jpeg_qf}', f'gaussian_{gaussian_var}', 'origin']
+        if args.identity_only:
+            stego_with_attack = [stego_img]
+            stego_with_attack_path = [save_stego_path]
+            attack = ['origin']
+        else:
+            stego_with_attack = [stego_jpeg_img, stego_g_noisy_img, stego_img]  # stego_jpeg_img,
+            stego_with_attack_path = [save_jpeg_path, save_g_noisy_path, save_stego_path]  # save_jpeg_path,
+            attack = [f'jpeg_{jpeg_qf}', f'gaussian_{gaussian_var}', 'origin']
 
         for i_attack, img_attack in enumerate(stego_with_attack):
 
@@ -257,6 +280,7 @@ if __name__ == '__main__':
                                                                      run_baseline=True,
                                                                      guidance_scale=1,
                                                                      init_image=img_attack,
+                                                                     steps=args.steps,
                                                                      )
 
                 """从恢复噪声中提取indice部分"""
