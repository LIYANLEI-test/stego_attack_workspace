---
context: default
phase: Evaluation And Analysis
task: selected-quality-budget-formal-queue
total_tasks: 18
status: running
last_updated: 2026-05-27T10:08:00+08:00
---

# Continue Here - Selected Quality-Budget Formal Queue

The current priority is to keep the formal selected attack queue running until
it either completes all selected jobs or logs a real failure.

## Current Queue

Root:

```text
/data2/liyanlei/stego_attack_data/attack_runs/selected_quality_budget_20260527
```

Driver:

```text
pid: 8246
log: /data2/liyanlei/stego_attack_data/attack_runs/selected_quality_budget_20260527/logs/queue_driver.log
```

Monitor:

```text
pid: 23026
log: /data2/liyanlei/stego_attack_data/attack_runs/selected_quality_budget_20260527/logs/queue_monitor.log
```

The queue is resumable. If it exits before all targets are complete, inspect the
failed job log under `logs/`, fix the runner if needed, and rerun
`scripts/run_selected_attack_queue.py` without `--force`; completed output dirs
will be skipped.

## Monitor

Use this command to refresh live summary/delta/tables and write progress
snapshots:

```sh
env -u LD_LIBRARY_PATH PATH=/data2/liyanlei/envs/stego_attack/bin:$PATH \
  /data2/liyanlei/envs/stego_attack/bin/python \
  scripts/monitor_selected_attack_queue.py \
  --root /data2/liyanlei/stego_attack_data/attack_runs/selected_quality_budget_20260527 \
  --queue-pid 8246 \
  --poll-seconds 300 \
  --finalize-when-done
```

Live artifacts:

```text
selected_attack_summary_live.csv
selected_attack_deltas_live.csv
paper_tables_live.md
paper_tables_live.tex
experiment_manifest_live.json
selected_attack_paper_audit_live.csv
selected_attack_paper_audit_live.md
queue_progress_snapshot.json
queue_progress_snapshot.md
```

Final artifacts are written without `_live` when all jobs complete.

## Formal Evaluation Contract

- Parameter selection used calibration sample indices `0-9`; paper tables and
  deltas exclude those indices by default.
- Raw queue output remains `100/500/50` samples for traceability, while
  formal held-out counts are CRoSS `90`, GSD/MAS/GRDH/Pulsar `490`, and MDDM
  pilot `40`.
- A failed native reveal/recovery counts as zero payload recovery only when a
  measurable saved stego/attacked image pair exists. Other failures are
  `unscorable_failures` and block a completed paper row.
- Final summary must recompute LPIPS and audit complete PSNR/LPIPS coverage
  before a row is used for fixed-quality-budget claims.

Latest monitored progress at 2026-05-27 13:07 CST:

```text
raw generated records: 2173/6550
formal held-out records: 2083/6370
completed jobs: 5/18
```

## Claim Boundaries

- Main-table candidates: CRoSS, GSD CIFAR10, MAS/GRDH, Pulsar.
- MDDM 128-byte remains `native_third_party` pilot/appendix.
- RGS remains identity-only unless the user accepts the attack runtime.
- Diffusion-Stego remains excluded from image-attack claims because the current
  path is projection-only.
- Regen-VAE and UnMarker are adapted attack baselines, not full paper
  reproductions.

## Next Action

1. Keep monitoring the queue.
2. If any job exits nonzero, inspect its log and patch the runner before
   resuming.
3. When the queue completes, generate final reports and `experiment_manifest.json`.
4. Confirm every claim-bearing row passes the fixed PSNR/LPIPS budget; do not
   retune parameters on held-out results.
5. Update `.planning/STATE.md`, commit, and push to GitHub.
