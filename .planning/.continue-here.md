---
context: default
phase: Evaluation And Analysis
task: quality-budget-attack-smoke-validation
total_tasks: 12
status: paused_by_user
last_updated: 2026-06-05T02:05:00+08:00
---

# Continue Here - Quality-Budget Attack Smoke Validation

# BLOCKING CONSTRAINTS - Read Before Anything Else

- [ ] CONSTRAINT: No automatic full-scale queues - the user clarified on
  2026-05-27 that the current task was only to test 10 samples to see whether
  attack candidates run and satisfy the quality-budget protocol. Do not resume
  or launch 500/6550-scale queues without explicit user approval.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Oversized formal run from stale handoff | A prior handoff said to continue the formal selected queue, but the user expected 10-sample smoke validation. This led to a 6550-record planned queue being continued. | blocking | Before any long run, confirm the intended sample count and write it into `.planning/HANDOFF.json`; default to 10 samples for candidate attack validation. |

## Stopped Queue

Root:

```text
/data2/liyanlei/stego_attack_data/attack_runs/selected_quality_budget_20260527
```

The queue was stopped at the user's request at approximately 2026-05-27
13:12 CST. Matching selected queue, monitor, and runner processes were killed:
driver `8246`, monitor `23026`, and active runner PIDs `2672`, `10521`,
`26258`, and `31750`. Follow-up checks with `ps` and `nvidia-smi
--query-compute-apps` showed no remaining matching workloads.

Latest monitored progress before stop:

```text
raw generated records: 2173/6550
formal held-out records: 2083/6370
completed jobs: 5/18
```

Treat this as accidental partial checkpoint data, not as the active target.
Do not resume this root without explicit user approval.

## Formal Evaluation Contract

- Parameter selection used calibration sample indices `0-9`; paper tables and
  deltas exclude those indices by default.
- A failed native reveal/recovery counts as zero payload recovery only when a
  measurable saved stego/attacked image pair exists. Other failures are
  `unscorable_failures` and block a completed paper row.
- Final summary must recompute LPIPS and audit complete PSNR/LPIPS coverage
  before a row is used for fixed-quality-budget claims.

## Intended Current Scope

The user intended only 10-sample testing to verify whether stronger candidate
attack methods can run and satisfy the quality-budget protocol. Existing
10-sample smoke/calibration outputs should be summarized first. Any future
formal full run needs an explicit go-ahead.

## Claim Boundaries

- Main-table candidates remain CRoSS, GSD CIFAR10, MAS/GRDH, and Pulsar, but
  only after an approved full run.
- MDDM 128-byte remains `native_third_party` pilot/appendix.
- RGS remains identity-only unless the user accepts the attack runtime.
- Diffusion-Stego has been removed from the active project because the old path
  was projection-only and not a full image-generation/reveal baseline.
- Regen-VAE and UnMarker are adapted attack baselines, not full paper
  reproductions.

## Next Action

1. Do not restart the stopped formal queue.
2. Use `docs/target_psnr_attack_comparison_20260605.md` as the current
   advisor-facing PSNR=30 comparison from the 10-sample bit-payload calibration
   evidence.
3. For Pulsar, report positive-capacity failure rate rather than treating
   zero-capacity raw result rows as meaningful recovery.
4. For Pulsar, distinguish native `uint16` storage from `uint8` image-channel
   attacks; the 2026-05-28 controls show both `uint8` storage and
   `uint16`-preserving resize/blur break all 10 positive-payload samples.
5. For comparisons to the ADS Pulsar-defense paper, use the separate raw
   Pulsar BER protocol: church-256, DDIM50, `E*64` key, 8192-byte payload, and
   `BER <= 0.48` as paper-style success. The 10-sample calibration matched
   the paper's identity/JPEG/resize behavior closely enough to rule out a
   gross Pulsar integration error.
6. Keep the paper framework docs explicit that full-scale execution is gated
   after smoke validation and user approval.
7. If the next task is to formalize equal-quality results, tune only missing
   10-sample candidate grids first: Pulsar needs harsher/finer PSNR-30
   candidates, and UnMarker needs a PSNR-30 profile before main-table use.
8. Keep CRoSS and RGS out of the current main attack comparison because the
   user asked to focus on hidden bit information rather than image payloads.
